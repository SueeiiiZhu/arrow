{
  "name": "@ea/tools",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild:core": "pnpm -F @ea/core build",
    "solve:big": "pnpm prebuild:core && node src/solve-big.mjs",
    "solve:all": "pnpm prebuild:core && node src/solve-all.mjs",
    "analyze:void": "pnpm prebuild:core && node src/analyze-head-void.mjs",
    "stat:corpus": "node src/stat-corpus.mjs",
    "generate": "pnpm prebuild:core && node src/generate.mjs",
    "typecheck": "echo 'no ts in tools package'"
  },
  "dependencies": {
    "@ea/core": "workspace:*"
  }
}
